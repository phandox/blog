###### Generated by Ansible, don't edit by hand! ######
{# Main context #}
user nginx nginx; # Container uses nginx as user
worker_processes 1; # container has only 1 CPU assigned

error_log /var/log/nginx-error.log info; # Global error log of nginx - will send to log management later
pid       /var/run/nginx.pid;


events {
    worker_connections 1024; # TODO find out what this settings do
}

{# Context for web server #}

http {
    include     /etc/nginx/mime.types;
    default_type application/octet-stream;

    access_log /var/log/nginx/access.log    combined;
    sendfile    on;

    keepalive_timeout   65;

    server {
        listen 80;
        server_name {{ blog_hostname }} www.{{ blog_hostname }};
        location / {
            root  {{ webcontent_dir }}/blog/;
        }
        
    }
}